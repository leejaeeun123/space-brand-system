# 모드라운지 로고 시스템 (FINAL)

> **🟢 2026-07-05 전면 개정 — 이 문서가 로고의 최종 진실의 원천이다.**
> 이전 확정본(2026-06-28 솔리드 팬 심볼, v8)은 **폐기**. 재은 결정으로 심볼을 없애고
> **심플 산세리프 워드마크 단독**으로 전환 → “레이어드 E” 워드마크로 확정.
> 발산~수렴 과정: 심볼 계열 `logo-concepts-v2~v8.html`(폐기), 워드마크 탐색 `v9~v17`, **확정 `v18-final.html`**.

## 1. 결정 요약

| 항목 | 확정 |
|---|---|
| 심볼 | **없음** — 워드마크 자체가 로고 (별도 심볼/픽토그램 제작 안 함) |
| 폰트 | **Paperlogy** 단일 (무료 상업 · fonts-archive) — 로고·디스플레이·본문 공통 |
| 워드마크 | 영문 `MODE LOUNGE` / 국문 `모드라운지` |
| 로고 웨이트 | **800** |
| 시그니처 장치 | **레이어드 E** — MODE의 E 세로획을 없앤 **가로 3선(동일폭)** = 쌓인 층 |
| 컬러 | 모노크롬 — 라이트=잉크 `#16130F`, 다크·오렌지=화이트 `#ffffff` |
| 컨셉 | “넘나드는·레이어드” 내장. 3선 = **WORK·CLASS·GATHER**가 쌓인 층 |

## 2. 시그니처 — 레이어드 E

- MODE의 **E에서 세로 스템(│)을 제거**하고 **가로 3선만** 남긴다 → “층을 쌓는(레이어드)” 개념을 워드마크에 내장.
- **3선 동일폭**(B안 확정). 위·중간·아래 3개 막대가 같은 길이 → 일반 E보다 “쌓인 층”으로 읽힘.
- 문맥(MOD_ + LOUNGE)에서 자연스럽게 **“MODE”로 읽힌다**.
- **의미 이중성**: 3선 = 3모드(WORK·CLASS·GATHER)가 겹겹이 쌓인 하나의 공간.

### E 커스텀 사양 (vs Paperlogy 원본 E)
- 대문자 cap-height 기준 3개 가로 막대, **세로 스템 없음**.
- **막대 두께 = 14** (viewBox `0 0 56 70` 기준; 본체 800 스템과 무게 일치).
- 막대 y 위치: `0 / 28 / 56` (두께 14, 균등 간격 14).
- 폭 = 56 (viewBox), 렌더 시 `height:.70em · width:.56em`.
- **D와의 광학 여백** `margin-left: 0.115em` — 시스템 자간(−2%)과 별개로 E만 살짝 띄운다(붙어 보임 방지).
- 최종 벡터화 시 이 비율로 **커스텀 아웃라인**(패스) 제작 → 폰트 의존성 제거.

## 3. 워드마크 · 락업

- **영문** `MODE LOUNGE` · Paperlogy **800** · 자간 **−2%**.
- **국문** `모드라운지` · Paperlogy **800** · 자간 **0%**.
- **프라이머리 = 세로 2줄** `MODE` / `LOUNGE` · **가운데 정렬** (레이아웃 원칙과 일치).
- **베리에이션 = 가로 1줄** `MODE LOUNGE` — 배너·웹 헤더·명함용.
- 국·영 병기 시 국문 위/영문 아래(또는 맥락에 따라 택1). 사이니지는 영문 워드마크 중심.

## 4. 컬러 사용

| 배경 | 워드마크(국·영·3선) |
|---|---|
| 크림/라이트 | 잉크 `#16130F` |
| 잉크/다크 | 화이트 `#ffffff` |
| 오렌지 `#E2531E` | 화이트 `#ffffff` |

※ 라이트/다크 버전은 **형태·비율·정렬 100% 동일**, 채움색만 반전. 3선도 본체와 같은 색.

## 5. 클리어스페이스 · 금지

- **클리어스페이스**: 대문자 높이(cap)의 약 1/2을 사방 여백으로.
- **하지 말 것**: 비율 변형/기울임, 3선 개수·두께·간격 임의 변경, E에 세로 스템 되살리기, 채움색 외 컬러 적용, 3선을 서로 다른 폭으로(동일폭 원칙 위반), 폰트 교체.

## 6. 사이니지 적용

- 아크릴 사이니지 표준: 상단 `3F · 멀티유즈 라운지` / 중앙 `MODE LOUNGE`(세로 2줄) / 하단 `WORK · CLASS · GATHER`.
- 반투명 아크릴 + 트레이싱지 인쇄 부착/투명 스티커(각인 비용 대체) — “레이어드” 질감과 개념적으로 일치.
- 자세한 소재·레이어드 방식 → `05-design-system/concept-keywords.md` 참조.

## 7. 폐기된 에셋 (심볼 계열)

> 아래는 **더 이상 사용하지 않는다**(솔리드 팬 심볼 폐기). 이력 보존용으로만 남김.
- `logo-symbol.svg` · `logo-symbol-white.svg`
- `logo-lockup-h.svg` · `-h-white.svg` · `logo-lockup-v.svg` · `-v-white.svg`
- `favicon.svg`(심볼 기반) → 워드마크 기반으로 재제작 필요
- `logo-concepts-v2~v8.html`(심볼 발산), `v9~v17`(워드마크 탐색)

## 8. 남은 작업

- [ ] 레이어드 E **커스텀 벡터(SVG/AI) 아웃라인** — 두께 14 / y 0·28·56 / 여백 0.115em 반영, Paperlogy 800 텍스트를 아웃라인 변환해 결합
- [ ] 워드마크 기반 **파비콘/앱 아이콘** 재제작(3선 모티프)
- [ ] 사이니지용 단색 각인판(트레이싱지/아크릴 사양)
- [ ] 락업 마스터 파일(.ai/.svg) — 세로·가로 각 라이트/화이트

## 9. 폰트 임베드 / 납품 주의

- 미리보기 HTML은 Paperlogy를 CDN(`fonts-archive`)으로 로드. **최종 인쇄·간판 납품 시 Paperlogy 텍스트를 아웃라인(패스) 변환**해 폰트 의존성 제거.
- Paperlogy: 무료 상업 사용 가능. 웨이트 100~900.
